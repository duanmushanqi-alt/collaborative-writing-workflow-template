# 协作写作项目执行规则

开始工作前，先读取：

1. 00-项目管理/项目设置.md
2. 00-项目管理/项目状态.md
3. 00-项目管理/窗口登记.md
4. 00-项目管理/协作流程.md

## 模式判断

- 项目模式为“模板”时，只能维护通用流程和空白模板，不得写入具体书名、大纲、资料或正文。
- 项目模式为“正式项目”时，按照协作流程处理具体作品。
- 从模板生成新仓库后，必须先执行“初始化新书项目”，不得跳过模式切换。

## 唯一状态源

00-项目管理/项目状态.md 是当前项目状态的唯一正式来源。

其他文件可以保存审核结论、历史过程和章节实际结果，但不得另行建立互相竞争的“当前状态”。发现不一致时，先核对事实，再修正项目状态。

## 文件所有权

- 项目管理窗口：负责项目初始化、Git恢复点、项目状态与窗口登记的一致性检查、流程维护、经验归并和阶段复盘；不作为每章日常启动或窗口创建的必经入口。
- 大纲窗口：维护正式大纲、简版大纲和写作规范，负责启动章节、方案审核、写作任务轻量检查和正文终审。
- 章节窗口：章节要求、参考资料、正文写作任务单、正文审核记录、章节正文和章节回填。
- 正文写作窗口：只修改对应章节的正文工作稿。
- 作者：创作方向、个人事实、声音和最终取舍。

不同窗口不得越权修改他人负责的文件。项目状态和窗口登记属于受控共享文件，只允许修改与自己当前任务直接相关的行或栏目；共享审核单中如有分区，严格按照分区填写。

## 窗口创建责任

- 项目初始化时，项目管理窗口创建第一个大纲窗口。
- 作者在大纲窗口说“启动第X章”后，当前大纲窗口创建该章空白工作包和章节窗口。
- 方案通过且正文写作任务单轻量检查通过后，当前章节窗口创建正文写作窗口。
- 正文写作窗口交稿后通知并返回当前章节窗口，不经过项目管理窗口中转。
- 长期窗口需要换代时，当前责任窗口创建自己的接班窗口；项目管理窗口只处理目标窗口丢失、重复登记或无法接管等异常恢复。
- 新建窗口必须自行读取材料、登记准确任务 ID，并更新自己负责的项目状态行；创建者不得替新窗口虚构登记信息。

## 阶段交接

任何窗口完成阶段任务后，必须连续完成：

1. 把稳定结果写入正式文件；
2. 更新项目状态中的对应行；
3. 按窗口登记找到下一责任窗口；
4. 发送只包含任务和文件位置的简短通知；
5. 确认下一窗口已经读取并接管。

需要新建下一工作窗口时，由协作流程指定的当前责任窗口直接创建。作者不负责复制完整材料，也不需要为阶段交接返回项目管理窗口。

如果当前环境不具备任务管理能力，不得声称已经通知；应明确给作者一条最短的人工接力指令。

## 写作安全

- 讨论中的探索性想法不自动写入正式文件。
- 正文工作稿与章节正文必须分开。
- 正文写作窗口不得直接覆盖章节正文。
- 被替换的正式版本先移入历史归档，再覆盖当前文件。
- 资料未核验时必须标注，不得写成确定事实。
- 任何章节未完成关闭检查，不得标记为“已关闭”。
