# 协作写作项目执行规则

开始工作前，先完整读取：

1. `00-项目管理/项目设置.md`
2. `00-项目管理/项目状态.md`
3. `00-项目管理/窗口登记.md`
4. `00-项目管理/协作流程.md`

## 模式判断

- “模板”模式只维护通用流程和空白模板，不得写入具体作品内容。
- “正式项目”模式按协作流程处理具体作品。
- 从模板生成新仓库后必须先执行“初始化新书项目”。

## 唯一状态源

`00-项目管理/项目状态.md` 是当前状态的唯一正式来源。审核单、正文审核记录和章节回填只保存内容、结论与历史，不得建立竞争性的当前状态。

## 文件职责

- 项目管理窗口：初始化、Git恢复点、状态与登记一致性、流程维护、经验归并、阶段复盘和异常恢复。
- 大纲窗口：全书结构、跨章节判断、方案审核、提示词轻检和正文终审。
- 章节窗口：七类章节文件、初稿验收、作者审读、章节回填和关闭通知。
- 可选资料寻回窗口：资料拆分、初审建议、台账和采用结果回收；不直接修改大纲或正文。
- 作者：方向、个人事实、声音和最终取舍。

不同窗口不得越权修改他人负责的内容。共享文件只修改与当前任务直接相关的行或分区。

## 阶段交接

每个阶段完成后，责任窗口连续执行：

1. 将稳定结果写入正式文件；
2. 更新唯一状态看板；
3. 按窗口登记找到下一责任窗口；
4. 发送只包含任务和文件位置的简短通知；
5. 确认下一窗口已读取并接管。

作者不复制完整材料。如果当前环境不能发送任务消息，必须如实说明，并只提供一条最短人工接力指令。

## 写作安全

- 探索性讨论不自动写入正式文件。
- 方案通过前不生成正式提示词。
- 初稿未达到C级，不交作者逐段审读。
- 未核验资料不得写成确定事实。
- 正文只保留一个当前认可版本；需保留的旧版移入历史归档。
- 方案审核、提示词检查和正文终审不得混写。
- 章节未完成关闭检查，不得标记“已关闭”。
- 模板仓库不得混入任何具体项目资产。

## 中文标题与命名

- 面向作者的目录、文件标题和窗口标题优先使用清楚的中文名称。
- 章节目录统一采用项目确认的一种格式，例如 `08-第八章`，不得混用数字章名、英文缩写和临时别名。
- 文件名表达职责，版本、日期和状态写入文件内容或Git记录，不在文件名上无限累加“最终版”“最新版”。
- 长期窗口换代只增加接班序号，不改变职责名称；当前接收窗口以 `窗口登记.md` 为准，不凭标题猜测。
